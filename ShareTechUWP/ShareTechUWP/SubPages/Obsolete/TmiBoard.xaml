<Page
    x:Class="ShareTechUWP.SubPages.Obsolete.TmiBoard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ShareTechUWP.SubPages.Obsolete"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Background="{ThemeResource SystemControlAcrylicWindowBrush}">

    <Grid>
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
                <Setter Property="FontSize" Value="30"/>
                <Setter Property="HorizontalAlignment" Value="Right"/>
                <Setter Property="Width" Value="80"/>
                <Setter Property="Height" Value="70"/>
                <Setter Property="VerticalAlignment" Value="Top"/>
            </Style>
        </Grid.Resources>
        <ScrollViewer Margin="55,35,160,30">
            <Grid x:Name="PostsGrid"/>
        </ScrollViewer>
        <Button Content="&#xE70F;" Margin="0,265,45,0" x:Name="WriteButton" Click="WriteButton_Click" />
        <Button Content="&#xE72C;" Margin="0,165,45,0" x:Name="RefreshButton" Click="RefreshButton_Click" />
    </Grid>
</Page>
